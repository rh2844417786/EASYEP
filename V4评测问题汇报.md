# DeepSeek-V4-Flash 评测问题汇报（诊断修订版）

> **原始汇报日期**：2026-08-13
>
> **修订状态**：已定位首要配置问题；待在 8×H100 服务器验证
>
> **目标**：先完成 V4-Flash 未剪枝基线，再采集 EASY-EP 统计

## 1. 关键结论

原报告把问题归因为“SGLang 对 FP4 无可用路径”，这个结论过强。当前 SGLang 已给出并验证 DeepSeek-V4-Flash 在 H100 上的原始 FP4 路线：

```text
8×H100 + TP=8 + --moe-runner-backend marlin
```

原实验只暴露 GPU 4–7，使用 `TP=4`，并落入 Triton fused MoE 路径；这不是 H100 的官方验证组合，足以解释 FP4 packed-K 被错误解释后出现的 `Hidden size mismatch`。应先按 TP=8 + Marlin 重跑，不能据 TP=4 的失败判定所有当前 SGLang 版本均不支持。

## 2. 更正后的模型与资源信息

| 属性 | 更正值 |
|---|---|
| 模型 | DeepSeek-V4-Flash，路径 `/mnt/public_data/deepseek-ai/DeepSeek-V4-Flash` |
| 参数 | 284B 总参数，约 13B 激活参数 |
| 结构 | 43 层；前三层 hash routing；256 routed experts；每 token 激活 6 个 |
| 精度 | routed experts FP4；attention/dense 等主要权重 FP8 |
| 路由 | `sqrtsoftplus`，`routed_scaling_factor=1.5` |
| H100 路线 | 单机 8×H100，TP=8，Marlin W4A16 |

“149GB 权重小于四卡 HBM 总和”不能推出“TP=4 完全支持”。模型运行还需要 KV/cache、CUDA graph、workspace、通信 buffer，并且内核对 TP 与权重布局有约束。

## 3. 已做工作与证据边界

| 步骤 | 已观察结果 | 能说明什么 |
|---|---|---|
| SGLang 0.5.13/0.5.15/nightly，TP=4 | `Hidden size mismatch` | 原 TP/backend 组合失败；不能证明 TP=8 + Marlin 失败 |
| 官方 `convert.py`，MP=4 | 约 2 分钟完成，产出约 157GB | 转换与加载入口可运行；不代表 forward kernel 正常 |
| 官方 FP4 forward | TileLang 编译/forward 无进展 | 官方示例栈在该容器/硬件组合仍需定位具体 kernel |
| tokenizer 修复 | 原 HF config + `trust_remote_code` 后可用 | 旧转换目录不能独立作为完整 HF tokenizer/config 来源 |

官方栈的“卡死”还没有足够日志证明是哪一个 kernel。此前只记录“编译 13 个/0 个 kernel”，没有逐层同步或 stage trace，因此 `fp4_gemm`、`hc_split_sinkhorn`、attention 等都只是候选，不应写成已确认根因。

## 4. 仓库中已加入的修复

- `tools/v4_preflight.py`：启动前检查 V4 config、8 卡可见性、空闲显存、TP 和 backend；
- `scripts/serve_v4_flash_h100.sh`：提供 eager smoke 与官方验证 topology 两种 profile；
- `scripts/smoke_v4_server.py`：最小 OpenAI-compatible 请求；
- `evaluation/run_sglang.py`：移除旧 SGLang DSL，修复 `0.0.0.0` 客户端地址、固定 32000 token、1000 线程、重复答案错配、路径与断点问题；
- `pruning/prepare_v4_calibration.py`：用 V4 官方 encoder/tokenizer 重建 calibration `input_ids`；
- `pruning/inf_v4.py`：在官方 V4 inference 模型上采集 43 层 EASY-EP 统计，并可逐层同步定位 forward 卡点；
- expert selection：取消 58 层硬编码，修复混合域脚本确定性崩溃与 `target_number` 无效；
- `model_prune.py`：从 checkpoint 推断层/专家并自动更新 config；对 V4 hash routing 主动拒绝不安全剪枝。

## 5. 服务器下一步

先不要使用已停止的 MP4 官方栈做 AIME。应先验证当前 SGLang 服务：

```bash
cd /path/to/easy-ep
export CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7
export MODEL_PATH=/mnt/public_data/deepseek-ai/DeepSeek-V4-Flash
PROFILE=smoke PORT=60000 bash scripts/serve_v4_flash_h100.sh
```

另一个终端：

```bash
python3 scripts/smoke_v4_server.py --base-url http://127.0.0.1:60000/v1
```

完整命令、统计采集、FP8 probe 旁路方案和验收门槛见 [`docs/deepseek_v4_flash_reproduction.md`](docs/deepseek_v4_flash_reproduction.md)。

## 6. 仍未解决的研究工程问题

V4 前三层依靠 token→expert hash 表。若全局把 `n_routed_experts` 从 256 改为 128，hash 表仍可能引用 128–255，旧 R1 物理剪枝方法会破坏模型。当前可以安全推进：原模型服务、基线评测、V4 内部统计与 mask 生成；但 quick-mask 的 V4 原生 SGLang 移植、hash 层策略及可加载的真实删权重 checkpoint 仍需要实现与服务器验证。

因此，在这些关卡完成前，不应宣称 DeepSeek-V4-Flash 已完成 EASY-EP 压缩或取得吞吐提升。
