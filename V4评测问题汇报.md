# DeepSeek-V4-Flash 评测问题汇报

> **汇报日期**: 2026-08-13
> **状态**: 实验暂停
> **目标**: 对 DeepSeek-V4-Flash 进行 AIME-24/25、HMMT 评测，记录时间/显存/指标

---

## 1. 模型信息（已确认）

| 属性 | 值 |
|---|---|
| **模型** | DeepSeek-V4-Flash（`/mnt/public_data/deepseek-ai/DeepSeek-V4-Flash`） |
| **参数量** | ~138B（MoE：256 路由专家，6 激活，43 层 + 3 hash 层 + nextn） |
| **权重大小** | 149GB（FP8 权重 + **FP4 专家** `expert_dtype=fp4`） |
| **架构** | DeepSeek-V4（新：sqrtsoftplus 路由、MHC 混合计算、index attention、输出 LoRA、稀疏注意力） |
| **能力** | ✅ 新一代模型，数学能力强（README 显示 MATH 57.4+），**有能力做 AIME 评测** |
| **显存** | ✅ 149GB < 640GB，**4 卡（GPU 4-7，tp=4）每卡 ~37GB 完全支持** |

## 2. 已完成的工作

| 步骤 | 状态 | 说明 |
|---|---|---|
| 环境准备 | ✅ | 创建 `wth333-v4` 容器（sglang:latest → nightly 镜像），GPU 4-7 |
| sglang 部署尝试 | ❌ | 所有 sglang 版本（0.5.13 / 0.5.15 / nightly）均失败 |
| 官方推理栈 | ⏸ | 转换成功，但推理 forward 卡死 |
| 权重转换 | ✅ | 官方 `convert.py` 成功（**2 分 2 秒**），输出 4 个 MP 分片（157GB，`/mnt/docker_data/v4-converted`） |
| tokenizer | ✅ | 修复（需原始 HF config + `trust_remote_code`） |
| hadamard 依赖 | ✅ | fast_hadamard_transform 编译失败 → torch 实现替代（已验证正确） |

## 3. 遇到的问题（核心障碍）

### 3.1 sglang 无法运行 V4-Flash（FP4 MoE bug）

所有 sglang 版本（0.5.13、0.5.15、nightly dev）加载 V4-Flash 后，CUDA graph 捕获/推理时崩溃：

```
AssertionError: Hidden size mismatch
File ".../fused_moe.py", line 858
    hidden_states.shape[1] == w1.shape[2] - padded_size
```

**根因**：V4-Flash 的专家是 **FP4**（`expert_dtype=fp4`），FP4 权重的 K 维度是 `hidden/2`（2048，4bit 打包）。但 sglang 检测到 `is_fp4_experts=True` 后，MoE 路径**未走 int4 分支**，按 FP8 处理（K 维度 4096）→ 断言失败。

**尝试**：nightly 镜像（最新 dev）→ 同样崩溃。判断为 sglang 对 V4-Flash FP4 支持不完整，无参数可绕。

### 3.2 官方推理栈 forward 卡死（tilelang）

使用 V4-Flash 自带官方推理栈（`inference/`：`convert.py` + `generate.py` + `model.py` + `kernel.py`）：
- ✅ 依赖满足：torch 2.11 / transformers 5.12 / tilelang
- ✅ 权重转换成功（2 分钟）
- ✅ 模型加载成功（157GB → 4 卡，每卡 ~41GB）
- ❌ **forward 卡死**：模型加载后执行 `model.forward()` 时 GPU 利用率 0%、CPU 低占用，无限挂起（测试 500-800 秒超时）

**tilelang 编译异常**：
| tilelang 版本 | 现象 |
|---|---|
| 0.1.11（nightly 自带） | 编译 13 个 kernel 后停滞，forward 卡死 |
| 0.1.8（官方要求版本） | 编译 0 个 kernel，forward 直接卡死 |

疑似 V4 的 tilelang kernel（`fp4_gemm` / `hc_split_sinkhorn` / `sparse_attn`）编译或运行挂起，与 GPU（H100）/ tilelang 版本存在兼容问题。

## 4. 当前状态

- **实验已暂停**，所有 V4 评测进程已停止
- **GPU 4-7 已释放**（全部 8 卡空闲）
- 中间产物保留：
  - 转换权重：`/mnt/docker_data/v4-converted`（157GB，4 个 MP 分片）
  - 容器：`wth333-v4`（已停止，基于 sglang nightly，装好 tilelang 0.1.8 / fast_hadamard 替代）
  - 评估脚本：`/tmp/eval_v4.py`、`/tmp/fwd_test.py`（容器内）

## 5. 建议下一步（可选方向）

1. **继续调试官方推理栈**：定位 tilelang 编译/forward 卡死的具体 kernel（需 GPU 4-7 + 诊断工具，可能涉及 tilelang 版本精确匹配或 GPU 特定配置）
2. **寻找支持 V4-Flash 的其他推理框架**：如 DeepSeek 官方 serve（如有）、vLLM 新版本（宿主有 `vllm/vllm-openai:v0.8.4` 等镜像，可试）、SGLang 更新版本（若 GitHub 有新 release）
3. **换更大显存/更新 GPU 环境**：官方栈的 tilelang kernel 可能需要特定 CUDA 版本或 GPU 架构
4. **降级方案**：如果评测 V4-Flash 持续受阻，可考虑模型转换（FP4→FP8）后尝试，或改用其他可运行的 DeepSeek 模型

---

*本文件由实验过程自动生成，作为 V4-Flash 评测遇到的问题记录*
